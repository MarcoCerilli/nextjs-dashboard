import DashboardSkeleton from "../../ui/skeletons";

export default function Loading() {
  return <DashboardSkeleton />;
}

//Questo file è stato creato automaticamente da Next.js per mostrare uno stato di caricamento durante il caricamento della dashboard.
//Non contiene logica personalizzata ed è principalmente un segnaposto visivo.Basato su Suspense di React, questo componente
// viene visualizzato mentre i dati della dashboard vengono recuperati in background.
